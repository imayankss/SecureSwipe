# SecureSwipe incremental execution ledger

This ledger starts after the read-only Gate 0 checkpoint. Each status is scoped
to the named micro-task; a completed card does not imply that its broad gate is
green.

## 2026-08-25 — Micro-task 1: model provenance

- **Gate/status:** P0-A card 1 — DONE (provenance decision complete; P0-A is not yet green).
- **Repository baseline:** `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`.
- **Commit:** Not committed. The worktree already contained unrelated untracked paths; this ledger is the only intended repository change from this card.
- **Candidate decision:** Use `artifacts/historical-reference-demo-v1/manifest.json` as the sole candidate for subsequent readiness verification, under the exact evidence category `historical_reference_demo_inference`.
- **Rejected candidate:** Do not use `artifacts/recovered-xgboost-20260624-demo-v1/manifest.json`. Its component bytes are hash-identifiable, but it is bundle format v2, lacks the v3 typed provenance contract, fails the current loader, and cannot be linked to the cited source commit or locked historical metrics.
- **Commands run:** `graphify query "model bundle provenance manifest historical reference training fingerprint source artifact quarantine metric" --dfs --budget 5000`; SHA-256 recomputation for both manifests, payloads, sidecars, recipe, and quarantine evidence; v3 `load_model_bundle(...)` plus `probe_bundle_runtime(...)`; Git commit/tree/history inspection for `bda9cea1de1575e74245b3f5b15a6dac69b88bab`; retained-input inventory beneath `/Users/mayanksuryavanshi/Downloads`; focused pytest command below.
- **Tests:** `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/test_historical_reference.py tests/test_model_bundle.py` — PASS, 63 tests.
- **Key artifacts:** historical-reference manifest SHA-256 `e355834d916ab3951e3069fc38ce286dd3e3abe4251c8643c4d859cd781bbbf0`; XGBoost payload SHA-256 `5ce63f1a7efa5625fbaa61177e76a548fd9ccc1c3f0a1530ccff835cf8b1dc73`; preprocessor SHA-256 `07d4a9f49448b6aa09a41c5c71dbaff5172a5fb5c870154d284671f323c7862f`; tracked recipe SHA-256 `55670d71abd7a52131fe58576de1771b2b381ba8cdd085f9e4c420dd9ba0c5dd`; quarantine anchor SHA-256 `f859c5bb470feb512de43ed5c531628d87ce0bc27c2babf4904848bfad506f17`.
- **Verified facts:** The selected v3 manifest and sidecars match the retained payload bytes; the current loader and golden runtime probe accept a real `xgboost.sklearn.XGBClassifier`; its 30-feature schema, deterministic training-pool fingerprint, quarantine identity, recipe identity, policy, and model version are internally bound. The manifest sets `historical_taint=true`, `decision_eligible=false`, `evaluation_performed=false`, and `historical_metrics_claimed=false`; threshold-to-current-model linkage is explicitly unverified.
- **Claim boundary:** This proves byte identity and the repository-defined, quarantined reference-build lineage for genuine historical-reference demo inference. It does not independently authenticate the original Parquet source identities, reproduce the absent source inputs, link this model to AP/precision/recall, establish calibration, or authorize production decisions.
- **Remaining blocker:** Run P0-A card 2 against this exact manifest to verify application loading, readiness, and model-info behavior. The retained recipe still says `candidate_identity_status=pending_independent_verification`, and the four source Parquet inputs were not found in the searched Downloads scope, so independent source rerun remains unavailable.

## 2026-08-25 — Micro-task 2: bundle readiness

- **Gate/status:** P0-A card 2 — DONE (bundle loading/readiness/model-info verified; P0-A is not yet green).
- **Repository baseline:** `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`.
- **Commit:** Not committed. The pre-existing worktree contains unrelated untracked paths; this ledger remains the only intended repository change from cards 1–2.
- **Candidate:** Exact manifest `artifacts/historical-reference-demo-v1/manifest.json`, SHA-256 `e355834d916ab3951e3069fc38ce286dd3e3abe4251c8643c4d859cd781bbbf0`.
- **Commands run:** Started the real module-level FastAPI app with absolute `SECURESWIPE_ARTIFACT_ROOT` and `SECURESWIPE_BUNDLE_MANIFEST`, exercised `GET /health/live`, `GET /health/ready`, and `GET /v1/model-info` through `TestClient`, compared model-info field-for-field with the manifest, exercised an unconfigured control app, and ran `verified_model_row()` with the same environment.
- **Tests:** `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/test_api.py::test_liveness_readiness_and_model_info tests/test_api.py::test_unavailable_model_is_live_but_not_ready tests/test_project_audit.py::test_missing_model_is_explicitly_unavailable` — PASS, 3 tests.
- **Verified facts:** Configured startup passed manifest verification and runtime probing. Liveness returned HTTP 200/`live`; readiness returned HTTP 200/`ready` with model version `historical-reference-xgboost-20260624-demo-v1`; model-info returned HTTP 200 with schema `1.0`, bundle format `3`, raw-score semantics, threshold `0.53`, all 30 ordered features, training fingerprint `76e867c9809da64a34ee45e0895cae03b1aea233af5b901384cd6d958f5dac13`, and the exact historical-taint/non-decision flags from the manifest. Supplied request IDs were preserved in response headers and redacted request logs.
- **Control evidence:** With no manifest configured, liveness remained HTTP 200 while readiness returned HTTP 503/`not_ready`; model-info returned HTTP 503 with schema `1.0`, the supplied request ID, and error code `model_unavailable`. The project audit classified the configured candidate `PASS`.
- **Claim boundary:** The selected local bundle is loadable and the reference API reports truthful readiness and model metadata. This does not yet prove prediction execution, direct/single/batch parity, malformed-request behavior, or missing/corrupt-bundle startup behavior for the complete P0-A gate.
- **Remaining blocker:** Run P0-A card 3 against this exact manifest for genuine detector execution and parity plus malformed and missing/corrupt-bundle behavior.

## 2026-08-25 — Micro-task 3: detector parity

- **Gate/status:** P0-A card 3 — DONE. The three P0-A cards now satisfy the detector-proof acceptance criteria for the bounded `historical_reference_demo_inference` evidence category; they do not establish production eligibility or historical-metric linkage.
- **Repository baseline:** `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`.
- **Commit:** Not committed. The pre-existing worktree contains unrelated untracked paths, including artifact-packaging work; this card's coherent tracked diff and ledger update remain uncommitted to avoid mixing ownership.
- **Candidate/tolerance:** Exact manifest `artifacts/historical-reference-demo-v1/manifest.json`, SHA-256 `e355834d916ab3951e3069fc38ce286dd3e3abe4251c8643c4d859cd781bbbf0`; the parity tolerance is the manifest golden-probe tolerance, absolute `1e-12`.
- **Repair:** Prediction responses now expose bundle format plus an explicit provenance object containing training fingerprint, evidence category, historical taint, decision eligibility, historical-metric-claim, and evaluation flags. Decision values are bounded to `human_review` and `below_review_threshold`; the ambiguous authorization-like value `pass` was removed from detector responses and load-test validation.
- **Commands run:** Read-only Graphify DFS over the direct/model/scoring/prediction/batch/parity/failure/provenance path; exact-candidate direct `score_bundle_frame(...)` plus real FastAPI single/batch requests; malformed request; unconfigured inference; a corrupt copy under an isolated temporary directory; manifest SHA-256; focused Ruff format/lint, the repository's critical mypy target, and focused pytest.
- **Tests:** `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/test_risk_scoring.py tests/test_api.py tests/test_container_contract.py` — PASS, 55 tests. Scoped Ruff check/format — PASS. Repository critical mypy target with `--ignore-missing-imports` — PASS, 29 source files.
- **Parity evidence:** A real `xgboost.sklearn.XGBClassifier` returned raw/decision score `0.0072313775308430195` through the direct scorer, single endpoint, and both batch rows; every difference was zero and therefore within `1e-12`. The bounded outcome was `below_review_threshold`. Single and batch responses also matched on threshold `0.53`, model version, bundle format `3`, schema `1.0`, and provenance; supplied request IDs were preserved.
- **Failure evidence:** An unknown sensitive-looking field returned HTTP 422/`validation_error` without echoing its value. Unconfigured inference returned HTTP 503/`model_unavailable` and emitted no decision. Appending bytes to a temporary copy of the selected model caused startup verification to refuse service before inference.
- **Claim boundary:** The selected byte-verified historical-reference bundle executes a genuine XGBoost estimator consistently through direct, single-API, and batch-API paths, with truthful bounded output metadata and fail-closed controls. This does not authenticate the absent original Parquet inputs, make the bundle decision-eligible, prove calibration, connect it to locked AP/precision/recall, or authorize payment decisions.
- **Remaining blocker:** P0-A has no remaining detector-path blocker within its disclosed evidence category. Micro-task 4 must separately verify the existing cost arithmetic and fixtures before any cost-panel edit.

## 2026-08-25 — Micro-task 4: cost evidence

- **Gate/status:** P0-B card 4 — DONE (cost arithmetic and locked fixture verified; P0-B presentation is not yet green).
- **Repository baseline:** `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`.
- **Commit:** Not committed. The worktree still contains the intended uncommitted P0-A diff plus unrelated pre-existing untracked paths, so this test-only card cannot be isolated safely in a local commit.
- **Locked evidence:** `reports/final/historical_observation.lock.json` SHA-256 `ed55691d6c364c2606a2b21be40e60960f04ee808eee815889a465a2fcf74c85` verifies three files. The locked final JSON SHA-256 is `8fe0d96e9106cbe06f1632415c25fe0c59870231728553ec71109e1a7febc24f`; it records threshold `0.53`, TP/FP/FN/TN `62/27/12/42621`, precision `0.6966292134831461`, recall `0.8378378378378378`, and PR-AUC `0.8287848539773868`.
- **Arithmetic evidence:** The exported illustrative fixture reconciles to 42,722 rows and 89 reviews. At its current illustrative assumptions, the canonical engine computes review `89`, false-positive `270`, missed-fraud `1,200`, and residual caught-fraud `3,100`, totaling `4,659` in the stored USD reference basis or `386,697` at the fixed display-only INR conversion of 83.
- **Test repair:** Added explicit zero-cost, high-false-positive-cost, high-false-negative-cost, and recovery-rate boundary tests. Added a lock-to-export-to-canonical-engine integration test so the displayed aggregate fixture and production cost formula cannot silently diverge.
- **Commands/tests:** Read-only Graphify DFS of the cost pipeline; `scripts/verify_historical_observation.py` — PASS, three files verified; `scripts/export_web_data.py --check` — PASS; `tests/test_cost_analysis.py tests/test_export_web_data.py tests/test_historical_lock.py` — PASS, 30 tests; scoped Ruff check/format and production cost-module mypy — PASS.
- **Presentation findings (not repaired on this card):** The exact required disclaimer is absent. The panel labels assumptions as false-positive cost, false-negative cost, review cost, and recovery rate rather than the required review cost, legitimate-customer friction, missed-fraud loss, and chargeback handling. Threshold, precision, and recall appear elsewhere on the dashboard but not inside the cost panel. The existing copy does correctly reject business-savings and threshold-recommendation claims.
- **Claim boundary:** The arithmetic, aggregate-count linkage, and current illustrative total are verified. The assumptions remain hypothetical and are not Razorpay economics, a savings estimate, or evidence that `0.53` is cost-optimal. No locked historical value or cost-panel code was changed.
- **Remaining blocker:** Micro-task 5 must minimally correct the existing cost panel's assumption labels/inputs, in-panel locked metrics, and exact disclaimer, then rerun frontend behavioral and responsive checks under the repository-required Node runtime.

## 2026-08-25 — Micro-task 5: cost presentation

- **Gate/status:** P0-B card 5 — DONE. P0-B is green for the bounded illustrative cost/review panel.
- **Repository baseline:** `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`.
- **Commit:** Not committed. The worktree contains the preceding intended P0-A/P0-B changes plus unrelated pre-existing untracked paths, so this card cannot be committed without mixing worktree ownership.
- **Panel repair:** The existing panel now exposes four editable canonical INR assumptions: review cost ₹83 per flagged row, legitimate-customer friction ₹830 per false positive, missed-fraud loss ₹8,300 per false negative, and chargeback handling ₹4,150 per caught fraud. USD remains an optional fixed display-only conversion; no live FX is fetched.
- **Locked evidence shown:** The same panel displays threshold `0.53`, precision `69.66%`, recall `83.78%`, review volume `89`, and TP/FP/FN/TN `62/27/12/42,621`, all supplied by the hash-verified historical export. The formula and component cards reconcile to the unchanged illustrative total ₹386,697.
- **Claim control:** The exact required disclaimer is visible in the section, evidence badge, and total card: “Illustrative scenario — not Razorpay economics and not a production-optimal threshold.” Supporting copy also states that editing inputs changes arithmetic only and makes no savings, threshold-recommendation, or autonomous-decision claim.
- **Generated artifact:** `web/public/data/dashboard.json` was deterministically regenerated from the verified exporter and passes read-only `--check`; the historical lock and its source evidence were not changed.
- **Commands/tests:** Python cost/export/lock suite — PASS, 30 tests; scoped Python Ruff — PASS. Under Node `22.13.1`/npm `10.9.2`, frontend data check, ESLint, TypeScript, all 23 unit tests, and the production static build — PASS. Focused Chromium 375px cost-panel test — PASS, including required inputs/evidence and no horizontal overflow.
- **Visual verification:** A production-build 375px panel capture was inspected locally; labels wrap cleanly, all evidence and components remain legible, and no clipping or overlap was observed. The temporary screenshot was kept outside the repository.
- **Claim boundary:** The panel demonstrates editable arithmetic over one already-observed locked split. Its INR assumptions are hypothetical and do not represent Razorpay economics, live FX, expected savings, real merchant loss, or a cost-optimal production threshold.
- **Remaining blocker:** P0-B has no remaining blocker. Micro-task 6 must separately address reviewer-first README/dashboard evidence hierarchy, links, limitations, and responsive verification without redesigning the product.

## 2026-08-25 — Micro-task 6: reviewer-first evidence

- **Gate/status:** P0-C — DONE. README and dashboard now agree on the bounded reviewer-first story without a visual redesign.
- **Repository baseline:** `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`.
- **Commit:** Not committed. The worktree contains the preceding intended P0-A/P0-B changes plus unrelated pre-existing untracked paths, so this card cannot be committed without mixing worktree ownership.
- **First-minute alignment:** The dashboard now visibly identifies “Razorpay AI Builder Internship · Track 2: AI Risk Manager,” describes SecureSwipe as a defense-only human-review aid, and shows the static export, optional local API, bounded-result, and human-review path. The README uses the same posture and separates the local model-serving path from the statically exportable dashboard.
- **Evidence separation:** README and dashboard retain four explicit evidence classes—historical evaluation, genuine demo inference, synthetic plumbing test, and illustrative cost scenario. Genuine inference is now described as actual estimator execution whose API response carries its own provenance; the selected bundle is disclosed as `historical_reference_demo_inference`, historical-tainted, not decision-eligible, and not proven to have produced the locked metrics.
- **Contract repair:** `RiskScoreDemo` now validates the P0-A response contract (`human_review`/`below_review_threshold`, bundle format, and the complete provenance object) and displays a human-readable bounded decision plus the API evidence category and decision-eligibility flag. The old `review`/`pass` frontend assumption was removed from the relevant unit and E2E fixtures.
- **Links:** No unverified live deployment or pitch link was added. The three existing public dashboard destinations—the repository, final report, and dated synthetic benchmark report—resolved during review; all dashboard hash anchors resolved in both responsive viewports.
- **Commands/tests:** Read-only Graphify DFS over the README/dashboard/architecture/evidence/provenance/static-fallback path; Node `22.13.1` `npm test` — PASS (data check, ESLint, TypeScript, 23 unit tests); `npm run build` — PASS (static prerender); full Chromium E2E — PASS, 5 tests.
- **Responsive/browser evidence:** Production dashboard reviewed at 1280×720 and 375×812. Document scroll width equaled client width at both sizes, the reviewer-first heading and static-fallback copy remained visible, all internal targets existed, and browser console warnings/errors were empty. The automated E2E also asserted no page errors or failed static requests. The static content remained complete without requesting `/v1/predict`; the configured opt-in mock exercised the complete genuine-inference response separately.
- **Claim boundary:** P0-C proves local candidate wording, evidence taxonomy, static usefulness, responsive layout, link targets, and frontend/API contract agreement. It does not prove a public candidate deployment, deployment SHA, pitch availability, final CI, original-source authenticity, historical-metric linkage to the demo bundle, or production suitability.
- **Remaining blocker:** P0-C has no remaining blocker. Micro-task 7 must separately implement and verify P1-A tamper-evident append-only audit evidence and idempotency; no audit work has begun here.

## 2026-08-25 — Micro-task 7: audit integrity

- **Gate/status:** P1-A — DONE for the disclosed single-process reference-service boundary.
- **Repository baseline:** `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`.
- **Commit:** Not committed. The worktree contains the preceding intended P0 changes plus unrelated pre-existing untracked paths, including recovered-packager work, so this card cannot be committed without mixing ownership.
- **Audit implementation:** Added `api/audit.py`, an optional `SECURESWIPE_AUDIT_LOG` sink, and `scripts/verify_api_audit_log.py`. Successful single and batch inference writes canonical redacted NDJSON in append mode, one event per returned prediction, with request/event IDs, UTC time, audit/API schema versions, hashed request-idempotency and canonical-input digests, verified serialized model-artifact SHA-256/version, decision score/threshold/bounded decision, latency/status, previous hash, and event hash. An atomically replaced local count/head anchor makes tail deletion detectable.
- **Idempotency:** A valid `X-Request-ID` is the in-process idempotency key. Identical concurrent or later retries wait for or return the original response with `X-Idempotent-Replay: true`; different canonical input under the same ID returns HTTP 409/`idempotency_conflict`. The owner publishes the cached result only after audit append, so replay does not rescore or create another event.
- **Integrity evidence:** Tests verify canonical encoding and chain linkage; mutation, tail deletion, and reordering all fail verification; and the writer refuses to append after external tampering. The verifier also rejects malformed/noncanonical JSON, duplicate identities, broken previous hashes, event-hash mismatches, and anchor count/head mismatch.
- **Privacy evidence:** Audit records use a strict field allowlist and contain only digests for inputs/idempotency. Tests prove PAN, CVV, payload secrets, customer email, authorization/API-key/cookie values, raw feature names, and raw feature values are absent. Rejected extra sensitive fields create no inference audit event.
- **Real-candidate evidence:** The selected historical-reference XGBoost bundle produced one audit event for two identical requests. Both responses were HTTP 200 and byte-equivalent at the JSON-contract level; the replay header was `true`; scoring/event count remained one. The event recorded bounded decision `below_review_threshold`, score `0.009456783533096313`, and model fingerprint `5ce63f1a7efa5625fbaa61177e76a548fd9ccc1c3f0a1530ccff835cf8b1dc73`, exactly matching the verified manifest model artifact. The standalone verifier accepted the one-event chain.
- **Commands/tests:** Read-only Graphify DFS across request/scoring/logging/hash/verifier boundaries; scoped Ruff — PASS; critical `mypy --no-incremental --ignore-missing-imports api src/artifacts` — PASS, 8 source files; focused audit/API/bundle/project-setup pytest — PASS, 97 tests. A full Python-suite diagnostic reached 759 PASS with one unrelated failure in the pre-existing untracked `tests/test_package_recovered_demo_bundle.py`, whose packager constructs the already-obsolete incomplete bundle contract; this card did not repair that explicitly excluded packager.
- **Claim boundary:** This is **tamper-evident append-only audit evidence**, not immutable storage. The local anchor shares the log's trust domain and can be replaced by an attacker who controls both files. Idempotent replay is in-process and is lost on restart; there is no distributed registry, remote/WORM anchor, retention policy, authentication, or multi-replica ordering claim.
- **Remaining blocker:** P1-A has no remaining blocker within that disclosed boundary. Micro-task 8 must separately verify deterministic failure/recovery behavior, including audit-sink failure; none of that P1-B work was started here.

## 2026-08-25 — Micro-task 8: failure recovery

- **Gate/status:** P1-B — DONE for the local single-process reference-service boundary.
- **Repository baseline:** `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`.
- **Commit:** Not committed. The worktree contains the preceding intended changes plus unrelated pre-existing untracked paths, including the explicitly excluded recovered-packager work, so this card cannot be committed without mixing worktree ownership.
- **Failure matrix:** Missing model returns HTTP 503/`model_unavailable` with no decision; a corrupt configured bundle refuses startup before inference; a synchronized blocked scorer returns HTTP 504/`prediction_timeout` with no decision; saturated admission returns HTTP 503/`capacity_exceeded` with no decision and accepts a later bounded request after the worker releases capacity; and a required audit failure returns HTTP 503/`audit_unavailable` with no decision.
- **Audit recovery repair:** Audit integrity/I/O failures now use the stable `audit_unavailable` envelope without logging the downstream exception text. The failed owner reservation is removed, so a transient failure injected before append can retry the same request ID after recovery. The recovered result is cached for replay and produces exactly one audit event; a partial append or log/anchor mismatch remains fail-closed for operator recovery.
- **Determinism:** Timeout and capacity tests use `threading.Event`/`Condition` synchronization instead of sleep-based races. No circuit breaker is present: startup verification, request deadlines, non-queueing admission control, and mandatory audit append are independent boundaries, so no closed/open/half-open behavior is claimed or tested.
- **Genuine-model demonstration:** `.venv/bin/python scripts/demo_api_failure_recovery.py` loaded `artifacts/historical-reference-demo-v1/manifest.json`, injected one pre-append audit failure, returned 503/`audit_unavailable` with no decision, retried the same request ID to HTTP 200/`below_review_threshold`, replayed without another score/event, verified one audit event, and completed in `0.242` seconds against a 20-second ceiling.
- **Commands/tests:** Scoped Ruff format/lint — PASS. Focused missing/corrupt/timeout/capacity/audit-recovery tests — PASS, 6 tests. `tests/test_api.py tests/test_api_audit.py tests/test_model_bundle.py` — PASS, 87 tests. Critical `mypy --no-incremental --ignore-missing-imports api src/artifacts` — PASS, 8 source files. Broader Python suite excluding only the known unrelated untracked `tests/test_package_recovered_demo_bundle.py` — PASS, 755 tests.
- **Claim boundary:** The evidence proves bounded local failure responses and deterministic recovery from released capacity and a transient audit failure before append. It does not prove automatic recovery from partial audit writes, distributed audit/idempotency behavior, process crash recovery, multi-replica admission control, provider availability, or a production SLO.
- **Remaining blocker:** P1-B has no remaining blocker within the disclosed boundary. Micro-task 9 must separately measure the genuine-model API benchmark and observability/efficiency evidence; no benchmark or release work began here.

## 2026-08-25 — Micro-task 9: measured genuine-model benchmark

- **Gate/status:** P1-C — DONE for the disclosed local single-process, dirty-worktree evidence boundary.
- **Repository baseline/source identity:** Git HEAD `bc2fc8502f8479fbbc0b9f30a68d3eb1236df7d7` on `codex/recovered-demo-bundle`; the serving/harness binary diff against HEAD is SHA-256 `c64bc286a3344267942e930de1be03d780e3dd52153f23b6872b8c8615ee2103`, and the imported untracked `api/audit.py` is SHA-256 `8585b2fecb82aeeb28e0f18270b85cc2f0d033a42514a7300c3d5b71e3b7952b`. This is not represented as a clean committed release candidate.
- **Bundle/input identity:** Exact manifest SHA-256 `e355834d916ab3951e3069fc38ce286dd3e3abe4251c8643c4d859cd781bbbf0`; serialized XGBoost SHA-256 `5ce63f1a7efa5625fbaa61177e76a548fd9ccc1c3f0a1530ccff835cf8b1dc73`; preprocessor SHA-256 `07d4a9f49448b6aa09a41c5c71dbaff5172a5fb5c870154d284671f323c7862f`; version/format `historical-reference-xgboost-20260624-demo-v1`/`3`; training fingerprint `76e867c9809da64a34ee45e0895cae03b1aea233af5b901384cd6d958f5dac13`; bundle size 490,948 bytes. The fixed golden-probe input JSON is SHA-256 `924e5043cc9cd3301ca8779a03371c0beb2ffd3a8311c20388b3fd463184b0a0`.
- **Harness repair:** The loopback-only harness now recomputes and verifies local manifest/model/preprocessor hashes, separates invalid HTTP-200 contracts from non-2xx responses, measures throughput over the request window rather than readiness/warm-up overhead, samples peak process CPU and RSS, records CPU model plus exact serving/model package versions, and declares zero core-inference LLM tokens. The committed test validates the complete recorded identity and golden-probe contract in a clean clone, and additionally recomputes the bundle binding when the intentionally ignored authorized local artifact is present.
- **Measured workload/result:** One macOS 26.5.2/arm64 Apple M2 Uvicorn worker; one fixed synthetic golden-probe input; `POST /v1/predict`; concurrency 8; 500 measured requests after one warm-up. Result: 500 successful, 0 errors, 0 timeouts, 0 non-2xx, 0 invalid contracts, 0 transport errors; successful throughput 169.3493 requests/s; p50/p95/p99 44.6333/80.3703/308.4804 ms; maximum 317.4931 ms; concurrent liveness HTTP 200 in 10.5706 ms; peak sampled CPU 109.4%; peak RSS 117,488 KiB; bundle size 490,948 bytes.
- **Cold start/runtime:** Operator-captured process-start epoch to completed warm-up measured 5.8494 seconds. This is an end-to-end upper bound including readiness polling and client/harness startup, not server-internal initialization telemetry. Runtime: CPython 3.12.10, FastAPI 0.141.1, Uvicorn 0.52.2, HTTPX 0.28.1, Pydantic 2.13.4, XGBoost 3.3.0, scikit-learn 1.9.0, NumPy 2.5.2, pandas 3.0.5, joblib 1.5.3; system memory 8 GiB.
- **Artifacts:** Raw JSON `reports/operations/2026-08-25_genuine_model_api_benchmark.json` SHA-256 `f4c9023e8d9b86595fbebdee4becf36f26762e5311a7142daa98d7f9ca2054ff`; narrative and exact commands in `reports/operations/2026-08-25_genuine_model_api_benchmark.md`; fixed input in `reports/operations/2026-08-25_genuine_model_benchmark_input.json`.
- **Prior synthetic evidence:** The dated logistic-regression result is labeled synthetic serving-path plumbing evidence only and is not combined with this genuine-model run. No retained repository artifact matching the prompt's exact “prior 100-record synthetic batch” description was found; any such run has the same plumbing-only classification.
- **Commands/tests:** Read-only Graphify DFS across load/runtime/model/bundle/metrics paths; scoped Ruff — PASS; `tests/test_load_test.py tests/test_api.py` — PASS, 59 tests; critical `mypy --no-incremental --ignore-missing-imports scripts/run_local_load_test.py api src/artifacts` — PASS, 9 source files; broader Python suite excluding only the known unrelated untracked recovered-packager test — PASS, 758 tests; `git diff --check` — PASS.
- **Claim boundary:** Core model inference uses zero LLM tokens. This single fixed-input loopback run has no audit-persistence, TLS/authentication, container, external network, multi-node, representative-traffic, model-quality, production-readiness, 1,000-RPS, or 10,000-RPS claim. The observed p99 tail is preserved rather than hidden, and the benchmark must be rerun after a clean release commit.
- **Commit/remaining blocker:** Not committed at the time this card was written, because the cumulative intended work remained mixed with unrelated pre-existing untracked paths. P1-C has no remaining blocker within its disclosed boundary. Micro-task 10 must separately begin the release-candidate checks and freeze; no release, deployment, pitch, or submission work began here.
- **Superseded by Micro-task 10 (recorded 2026-08-25):** The "not committed" status above is historical. The intended cards 1–9 were subsequently isolated into commit `e3580e4caa7dc2b5b3205246427d1ea15f2cf554`, which is the release candidate. The benchmark numbers, bundle identity, and claim boundary recorded above remain accurate and were re-verified against that commit.

## 2026-08-25 — Micro-task 10: release-candidate checks

- **Gate/status:** Release gates — BLOCKED on one external gate. Every locally reproducible check is green on the exact candidate SHA; GitHub Actions has not run on that SHA because it is unpushed and pushing was not authorized in this session.
- **Release candidate identity:** Commit `e3580e4caa7dc2b5b3205246427d1ea15f2cf554` on `codex/recovered-demo-bundle`, tree `fa94e162c275ffb286930dd1bba2d57d1cb06023`. This supersedes the earlier isolated candidate `414d45778136505eab8047f0229a0643c3e44640`, which is a sibling commit that must not be released: it fails the committed benchmark-integrity test in a clean clone.
- **Release-critical defect found and fixed:** In a clean detached worktree at `414d4577`, `tests/test_load_test.py` failed (`757 passed, 1 failed`) because the benchmark-evidence test dereferenced `artifacts/historical-reference-demo-v1/manifest.json`, which is intentionally gitignored and therefore absent from any clean clone and from CI. The dirty local worktree had masked this. The fix on `e3580e4` validates the complete committed evidence contract from recorded values unconditionally, and recomputes the local bundle binding only when the authorized ignored artifact is present. No model weights were committed to satisfy the test.
- **Clean exact-SHA verification method:** All Python evidence below was produced in a detached `git worktree` at `e3580e4` under the scratchpad, confirmed to contain neither the ignored model bundle nor the unrelated untracked `tests/test_package_recovered_demo_bundle.py`, so no local dirty state could contaminate the result.
- **Python gates (clean worktree, exact SHA) — all PASS:** `pytest` — 758 passed, exit 0, including the previously failing benchmark test; `compileall api src scripts tests`; `ruff check api src scripts tests`; `mypy --ignore-missing-imports` over the 30 CI-listed critical source files; `mypy --no-incremental --follow-imports=skip scripts/run_reference_stage.py`; `pip check`; `export_web_data.py --check`; `verify_historical_observation.py` (`status=locked_historical_observation_verified`, 3 files); `create_synthetic_monitoring_demo.py --check`; `python -m build --no-isolation`; `verify_wheel_contents.py` (wheel SHA-256 `20cc351900c5e98444182927c84785ab9a81ac5bfc22c18e38f7bc5f38c41bcb`); `pip_audit` on `requirements/api-linux.lock` and `requirements/quality-linux.lock` — no known vulnerabilities in either.
- **Frontend gates (Node 22.13.1, the CI-pinned version) — all PASS:** `npm test` (data:check + eslint + `tsc --noEmit` + vitest, 23 tests passed); `npm run build` (compiled, all 4 routes prerendered static); `npm run test:e2e` (Playwright/Chromium, 5 passed, covering keyboard reachability, WCAG scan, mobile navigation, desktop and 375px overflow/broken-link/console-error checks, and one genuine live-inference response validation); `npm audit --audit-level=high` — 0 vulnerabilities. Node 20.18.3, the machine default, cannot run this suite (`ERR_REQUIRE_ESM` in vitest's jsdom chain); the pinned 22.13.1 is required.
- **Secret scan — PASS:** TruffleHog 3.96.0 (the CI-pinned version) over full Git history with `--exclude-detectors=Lob`: 2,348 chunks / 4,860,758 bytes scanned, **0 verified and 0 unverified secrets**. The only local env file, `web/.env.local`, is untracked and covered by `web/.gitignore`; its values were not read or recorded.
- **Container gate (linux/arm64, matching this host) — PASS:** Image built from the clean worktree with `VCS_REF=e3580e4…`, producing `sha256:e894e688a7d198cf8e7528428722ed9fb31d0d1d572fbbcc0db9a53fc65421f6` whose `org.opencontainers.image.revision` label equals the candidate SHA. Synthetic-only smoke bundle generated by the candidate runtime under `--network none --read-only --user` (directory SHA-256 `4a4beb0ae3ae92113a5e26ad75c738f32dbad019411609fab28c7dc50b8229d9`). Readiness in 1 s, liveness HTTP 200, and `/v1/predict` matched `smoke_expected.json` field-for-field under CI's exact assertion script. Runtime hardening verified in-container: `uid == 10001` and `pip` absent. Trivy 0.70.0 with the repository `.trivyignore.yaml`: **exit 0, 0 HIGH/CRITICAL vulnerabilities**.
- **Measurement-integrity correction:** A first smoke attempt was discarded, not reported. Its `docker run` failed with "port is already allocated" and the HTTP responses actually came from a stale pre-existing `secureswipe-api:local-final` container from 2026-08-20, identifiable because it returned `decision:"pass"` with no provenance block. The candidate image returns `decision:"below_review_threshold"` with the full provenance block. The smoke evidence above was re-run on port 8017 against the confirmed candidate container.
- **Deployed/static verification — PASS:** Production `next start` served HTTP 200 with `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy`, and CSP present and `X-Powered-By` absent. Desktop 1280×800 and mobile 375×812 both rendered with **zero console errors** and no page-level horizontal overflow (`document.scrollWidth == 375`); the wide elements at 375px are inside an intentional horizontally scrollable nav strip. The evidence legend, the locked historical values (42,722 holdout rows, 74 fraud cases, AP 0.8288, recall 83.78%), the exact illustrative disclaimer, and defense-only wording were all visible. Static fallback confirmed: this was rendered with no local API running.
- **CSP finding resolved as a non-defect:** An observed `connect-src 'self' http://127.0.0.1:3200` came from Playwright's `webServer` command, which rebuilds `.next` with `NEXT_PUBLIC_SECURESWIPE_API_URL` set. It was an artifact of running e2e before serving, not a property of the release build; a clean `npm run build` emits `connect-src 'self'`, which was verified directly.
- **Release hygiene — PASS:** `git diff --check` clean; no conflict markers; no model weights, bundle payloads, or raw datasets in the candidate tree. Deployment posture is unchanged and truthful: `README.md` states no public deployment is authorized or present for this candidate, and `docs/OPERATIONS.md:203` documents the model/bundle rollback path gated on explicit owner approval.
- **Not reproduced locally (platform-bound or unauthorized):** CodeQL (`python`, `javascript-typescript`); the `linux/amd64` container matrix leg (this host is arm64; QEMU emulation was not run); the Linux `--require-hashes` lock-install and clean-venv wheel-import proof; SBOM generation and upload. None of these is claimed as passing.
- **Claim boundary:** The candidate passes every check this machine can run, including the full Python suite, the full frontend suite, full-history secret scanning, and an arm64 container build/smoke/vulnerability scan. This is **not** a completed release freeze: the workflows have not run on `e3580e4`, so "GitHub Actions green on the exact candidate SHA" remains unproven. No deployment was performed and no submission claim is made.
- **Remaining blocker:** `e3580e4` is unpushed and no remote-tracking branch contains it; `gh` is not installed on this machine. Pushing is an external action requiring explicit owner approval per the operating model, so it was not performed. Micro-task 11 (submission package: claim-to-evidence matrix, pitch, and form answers) must not begin until the release evidence is frozen by a green CI run on the exact candidate SHA.

## 2026-08-25 — Micro-task 11: submission package

- **Gate/status:** Submission package — DONE for the claim-to-evidence matrix, pitch, form answers, and final checklist. The release itself is **not frozen**; the package is written so that nothing in it depends on an unproven CI result.
- **Artifacts:** `docs/evidence/CLAIM_TO_EVIDENCE_MATRIX.md` (commit `76f59b7`); `docs/evidence/SUBMISSION_PACKAGE.md` (commit `9a67d57`).
- **Matrix scope:** Eight claim groups — positioning, locked historical evaluation, genuine inference and provenance, fail-closed/audit/idempotency, measured performance, illustrative cost, engineering/security posture, and deployment status. Every row names a committed artifact, a named test, or a measured report, and every group carries an explicit "must not be claimed" boundary.
- **Facts re-verified before writing (not taken from memory):** locked metrics read from `reports/final/final_model_evaluation.json` (threshold 0.53; precision 0.6966; recall 0.8378; PR-AUC 0.8288; ROC-AUC 0.9613; TP/FP/FN/TN 62/27/12/42,621); fraud prevalence computed as 74/42,722 = 0.1732 %; bounded decisions confirmed from the `api/schemas.py` `Literal["human_review", "below_review_threshold"]`; stack versions read from `web/package.json` and `pyproject.toml`.
- **CI status recorded in the package:** Quality and Security green on the candidate content (merge commit `38703d0d`, tree `ed5ef8e8…`, byte-identical to candidate `e3580e4`). Container `linux/arm64` red. No "all CI green", live-URL, or deployment claim appears anywhere in the package.
- **Container arm64 diagnosis (failure mode confirmed, root cause NOT confirmed):** The job log shows the container reaching `INFO: Waiting for application startup.` and never emitting `Application startup complete.`; the smoke step's 90-attempt readiness loop therefore exhausts and the job exits 1 after ~92 s. The `linux/amd64` leg passes in the same run. The startup-path diff against the last passing commit `bc2fc85` is trivial: `src/artifacts/bundle.py` gains a `model_artifact_sha256` string field and its format validation, the audit subsystem is inert because the smoke sets no `SECURESWIPE_AUDIT_LOG`, and `ConcurrencyGate` is a counter behind a `threading.Lock` with no thread pool. The 5 s prediction deadline predates the candidate and is not the trigger.
- **Reproduction attempts (both negative):** Native arm64 — ready in 1 s, prediction 44 ms, all step-7 assertions and the uid/pip hardening check pass. Rosetta-emulated `linux/amd64` — bundle generation 7.9 s, ready in 8 s of a 90 s budget, prediction 59 ms, curl exit 0, assertions and hardening pass. Neither reproduces the CI hang; Docker Desktop's Rosetta path is far faster than CI's pure QEMU.
- **Leading hypothesis, explicitly unconfirmed:** OpenMP thread initialization hanging during model load under QEMU user-mode emulation; the image pins no `OMP_NUM_THREADS` or BLAS thread limits. Recorded as a hypothesis only. An earlier hypothesis (the 5 s deadline) was tested and disproved, and an earlier reading of Container run #17 as evidence of flakiness was withdrawn: run #17 failed on **both** legs from a distinct, already-fixed bundle-permissions bug, repaired by `bc2fc85`.
- **Decisive next test (not yet run):** Re-run the failed job. Passing indicates QEMU flakiness and no code change; failing identically indicates a deterministic defect warranting a targeted fix. No speculative fix was written, and no feature was added after the release-candidate commit.
- **Claim boundary:** The pitch and form answers are truthful against committed evidence as of this entry. They must not be delivered with an "all CI green" statement until the Container `linux/arm64` leg passes or the limitation is stated outright. Final form submission remains an owner action.
- **Remaining blocker:** Container `linux/arm64`. `main` currently carries that red check.

## 2026-08-25 — Release scope decision: container target is linux/amd64

- **Gate/status:** Release scope — DECIDED by the owner. ARM64/QEMU root-cause investigation is stopped as not submission-critical.
- **Reverted:** The speculative container thread-pinning commit `cabb8cb` was reverted in `75ff863`. `Dockerfile` is byte-identical to candidate `e3580e4` again. That change pinned `OMP_NUM_THREADS`/`OPENBLAS_NUM_THREADS`/`MKL_NUM_THREADS`/`NUMEXPR_NUM_THREADS` to 1 and did pass a full local smoke on both architectures with byte-identical output (`raw_score` `0.051735539252182494`), but it was written against an unproven diagnosis and is not being shipped on that basis.
- **Scope change:** `.github/workflows/container.yml` now builds, smokes, scans, and SBOMs `linux/amd64` only. The emulated `linux/arm64` matrix entry and the now-unnecessary `Set up QEMU` step were removed. The matrix carries an inline comment recording why the leg is deferred rather than silently dropped.
- **Unresolved issue, recorded not fixed:** Under QEMU user-mode emulation on GitHub-hosted runners the container reaches `INFO: Waiting for application startup.` and never emits `Application startup complete.`; the smoke step's 90-attempt readiness loop exhausts and the job exits 1 after ~92 s. Reproduced deterministically three times with distinct container IDs. It did not reproduce on native arm64 or Rosetta-emulated amd64. The startup-path diff against the last passing commit `bc2fc85` is trivial, and `linux/amd64` passes in the same runs. Root cause remains unknown.
- **Documentation updated to stop claiming arm64:** `docs/CONTAINER.md` gains a "Supported architectures" table plus the deferral rationale; `docs/LIMITATIONS.md` states the amd64-only release target and the deferral; `docs/DEPLOYMENT.md` no longer names `linux/arm64` as the build or provider-selection target; `docs/evidence/CLAIM_TO_EVIDENCE_MATRIX.md` adds row 7.9 and a scope note forbidding any arm64 support claim; `docs/evidence/SUBMISSION_PACKAGE.md` replaces its arm64 blocker with an amd64 scope statement and a checklist line forbidding multi-architecture claims.
- **Claim boundary:** The container release target is `linux/amd64` only. **No arm64 container support, multi-architecture image, or green arm64 CI leg may be claimed.** A native arm64 build remains a verified local development convenience and nothing more.
- **Remaining blocker:** None from arm64. The release gate now depends on Quality, Security, and the `linux/amd64` Container workflow passing on the submitted commit.
