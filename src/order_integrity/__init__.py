"""Synthetic, deterministic pre-model order-integrity reference.

It demonstrates server-side amount reconstruction and invariant checking. It is
not a live Razorpay integration, not evidence about any real incident, and not
part of SecureSwipe's fraud-model metrics.

Nothing here calls a model, reads a threshold, touches the fraud-scoring path,
opens a network connection, or authorises any financial action.
"""
