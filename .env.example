# SecureSwipe's current static demonstration deployment requires no environment variables.
# The optional local API audit path is server-only; create its parent directory first.
# SECURESWIPE_AUDIT_LOG=artifacts/audit/prediction-events.ndjson
